import type { NextPage } from 'next';
import React from 'react';
import Head from 'next/head';
import { useRouter } from 'next/router';

const ContainerModule: NextPage = ({ children, ...customeMeta }) => {};

export default ContainerModule;
